import { h } from 'preact';
import { TabPlaceholder } from './TabPlaceholder';

export function ProgressionTabPlaceholder() {
  return (
    <TabPlaceholder
      title="Progression"
      batch="T2b / T2d"
      blurb="Perks, level-up flow, enhancements, and FH crafting/brewing."
    />
  );
}
