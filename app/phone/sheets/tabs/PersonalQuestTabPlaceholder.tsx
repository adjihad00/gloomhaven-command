import { h } from 'preact';
import { TabPlaceholder } from './TabPlaceholder';

export function PersonalQuestTabPlaceholder() {
  return (
    <TabPlaceholder
      title="Personal Quest"
      batch="T2c"
      blurb="Quest card, progress markers, retirement conditions, retire action."
    />
  );
}
