{
  "name": "@gloomhaven-command/phone",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/main.ts --bundle --outfile=dist/main.js --format=esm",
    "watch": "esbuild src/main.ts --bundle --outfile=dist/main.js --format=esm --watch"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  }
}
